"use strict";
/*import mongoose, {Schema, Model} from "mongoose"


export interface RestaurantType {
    id:string,
    address: object,
    borough: string,
    cuisine: string,
    grades: Array<object>,
    name: string,
    restaurant_id: string
}

const restaurantSchema = new Schema({
    address: {
        building: String,
        coord: [Number],
        street: String,
        zipcode: String
    },
    borough: { type : String, required: true },
    cuisine: { type : String, required: true },
    grades: [{
        date: Date,
        grade: String,
        score: Number
    }],
    name: { type : String, required: true },
    restaurant_id: { type : String, required: true }
})

export const Restaurant = mongoose.model<RestaurantType>("restaurant", restaurantSchema)

*/
